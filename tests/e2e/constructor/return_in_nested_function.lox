// A local function named init inside a constructor is an ordinary function,
// so returning from it does not return from the constructor.
class Foo {
    init() {
        fun init() {
            return "bar";
        }
        this.nested = init();
    }
}

var foo = Foo();
assert(foo.nested == "bar");
assert(type(foo) == "instance");
