// Breaking out of a while loop closes captured locals.
var g;
var j = 0;

while (j < 10) {
    var y = j * 100;
    fun grab() {
        return y;
    }
    g = grab;
    if (j == 3) break;
    j = j + 1;
}

assert(g() == 300, "the closure kept the value from the iteration that broke");
