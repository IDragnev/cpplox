fun outer() {
    var x = "outside";
    fun inner() {
        return x;
    }
    return inner;
}

// The local outlives the call it was declared in.
var closure = outer();
assert(closure() == "outside");
