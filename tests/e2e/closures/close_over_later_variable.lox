// A closure captures every enclosing local it names, regardless of the order
// they were declared in or the order it reads them.
fun f() {
    var a = "a";
    var b = "b";
    fun g() {
        return b + a;
    }
    return g();
}

assert(f() == "ba");
