var f;

class Foo {
    method(param) {
        fun f_() {
            return param;
        }
        f = f_;
    }
}

// A method parameter is captured like any other local.
Foo().method("param");
assert(f() == "param");
