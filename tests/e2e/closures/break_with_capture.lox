// Breaking out of a for loop closes captured locals.
var f;

for (var i = 0; i < 10; i = i + 1) {
    var x = i * 10;
    fun capture() {
        return x;
    }
    f = capture;
    if (i == 2) break;
}

assert(f() == 20, "the closure kept the value from the iteration that broke");
