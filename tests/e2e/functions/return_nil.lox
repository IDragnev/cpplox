// A bare return gives nil.
fun f() {
    return;
}
assert(f() == nil);

fun g() {
    return nil;
}
assert(g() == nil);

// So does falling off the end.
fun h() {
    var x = 1;
}
assert(h() == nil);
