class A {
    init(param) {
        this.field = param;
    }

    test() {
        return this.field;
    }
}

// A subclass without its own init inherits the superclass constructor.
class B < A {}

var b = B("value");
assert(b.test() == "value");
assert(b.field == "value");
