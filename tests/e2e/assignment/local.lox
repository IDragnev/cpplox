{
    var a = "before";
    a = "after";
    assert(a == "after");

    // Assignment is an expression that evaluates to the assigned value.
    assert((a = "arg") == "arg");
    assert(a == "arg");
}
