// Break exits the loop immediately.
var trace = "";
var i = 0;
while (i < 10) {
    if (i == 3) break;
    trace = trace + str(i);
    i = i + 1;
}

assert(trace == "012", trace);
assert(i == 3, "break left the counter where it was");
