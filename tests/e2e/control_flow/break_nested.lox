// Break only exits the innermost loop, so the outer loop runs all three times.
var trace = "";
for (var i = 0; i < 3; i = i + 1) {
    for (var j = 0; j < 3; j = j + 1) {
        if (j == 1) break;
        trace = trace + str(i * 10 + j) + ",";
    }
}

assert(trace == "0,10,20,", trace);
