// The scanner produces a minus token regardless of the surrounding spaces, so
// none of these parse as a literal followed by a negative literal.
assert(1 - 1 == 0);
assert(1 -1 == 0);
assert(1- 1 == 0);
assert(1-1 == 0);

var a = 3;
assert(a -1 == 2);
