var a = "before";
a = "after";
assert(a == "after");

// A variable is not bound to the type it started with.
a = 1;
assert(a == 1);
assert(type(a) == "number");

// Assignment is an expression that evaluates to the assigned value.
assert((a = "arg") == "arg");
assert(a == "arg");
