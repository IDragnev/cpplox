// Until the shadowing declaration is reached, the name still refers to the
// outer variable.
{
    var a = "outer";
    {
        assert(a == "outer");
        var a = "inner";
        assert(a == "inner");
    }
    assert(a == "outer");
}
