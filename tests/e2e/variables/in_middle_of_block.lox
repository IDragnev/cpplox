// A declaration takes effect from its own statement onwards, so later
// declarations can read the earlier ones.
{
    var a = "a";
    assert(a == "a");
    var b = a + " b";
    assert(b == "a b");
    var c = a + " c";
    assert(c == "a c");
    var d = b + " d";
    assert(d == "a b d");
}
