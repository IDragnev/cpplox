class Thing {
    method() {
        return this;
    }
}

// this is the instance the method was called on, not a copy of it.
var thing = Thing();
assert(thing.method() == thing);
assert(str(thing.method()) == "<Thing instance>");
