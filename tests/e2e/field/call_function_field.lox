class Foo {}

fun bar(a, b) {
    return "bar" + str(a) + str(b);
}

// A field holding a function is called without binding the instance.
var foo = Foo();
foo.bar = bar;

assert(foo.bar(1, 2) == "bar12");
assert(foo.bar == bar);
