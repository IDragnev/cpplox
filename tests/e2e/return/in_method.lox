// Return leaves the method immediately, so the statement after it never runs.
var reachedEnd = false;

class Foo {
    method() {
        return "ok";
        reachedEnd = true;
    }
}

assert(Foo().method() == "ok");
assert(reachedEnd == false);
