// A reading is a count of milliseconds, so it is a number and never negative.
var start = clock();
assert(type(start) == "number");
assert(start >= 0);

// Time never goes backwards.
assert(clock() >= start);

// Natives are ordinary values.
fun twice(f) {
    f();
    return f();
}
assert(twice(clock) >= start);
